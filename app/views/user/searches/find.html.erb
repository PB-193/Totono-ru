
<h3 class="pt-3">このページでは様々な検索ができます</h3>

<div class="row mt-5">
  <div class="col-sm-5 ">
  <h6>絞り込み検索を行うことができます</h6>
  <h6>〜<strong>投稿日</strong>で検索ができます〜</h6>
    <%= form_with(url: searches_path, method: :get, local: true) do |f| %>
      <table class="table">
        <thead>
          <tr>
            <th>開始日</th>
            <th>終了日</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>
              <div class="field">
                <%= f.label(:start_date, "ここから") %>
                <%= f.date_field :start_date %>
              </div>
            </td>
            <td>
              <div class="field">
                <%= f.label(:end_date, "ここまで") %>
                <%= f.date_field :end_date %>
              </div>
            </td>
          </tr>
        </tbody>
      </table>

  <h6>〜<strong>訪問日</strong>で検索ができます〜</h6>
      <table class="table">
        <thead>
          <tr>
            <th>開始日</th>
            <th>終了日</th>
          </tr>
        </thead>
        <tbody>
            <tr>
              <td>
                <div class="field">
                  <%= f.label(:start_date, "ここから") %>
                  <%= f.date_field :start_date,value: :visit_day %>
                </div>
              </td>
              <td>
                <div class="field">
                  <%= f.label(:end_date, "ここまで") %>
                  <%= f.date_field :end_date, value: :visit_day %>
                </div>
              </td>
            </tr>
        </tbody>
      </table>
      <%= f.submit "検索する", class: "btn btn-info mb-2" %>
    <% end %>

  </div>

  
  
  <div class="col-sm-7">
    <h6>タグの検索を行うことができます</h6>
    <h6>タグのボタンをクリックすると、そのタグが付与された投稿一覧が表示されます</h6>
    <div class="tag-list">
      <%= form_tag delete_admin_tags_path, method: :delete do %>
        <table class="table">
          <thead>
            <tr>
              <th>タグ名</th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            <% @tags.each_slice(2) do |tags_row| %>
              <tr>
                <% tags_row.each do |tag| %>
                  <td><%= link_to tag.name, searches_path(tag_id: tag.id), class: "btn btn-primary" %></td>
                <% end %>
              </tr>
            <% end %>
          </tbody>
        </table>
      <% end %>
    </div>
  </div>
</div>
